function rot13(str) {
    //function rot13(inputString):
      //decodedString = ""
      //for each character in inputString:
         // if character is an alphabetic character:
             // apply ROT13 cipher to character
         // else:
             // add character to decodedString
      //return decodedString
    return str;
  }
  
  console.log(rot13("SERR PBQR PNZC"));